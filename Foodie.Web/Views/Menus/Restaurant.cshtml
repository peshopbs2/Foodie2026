@model Foodie.Models.ViewModels.Menus.ListMenusByRestaurantIdViewModel

@{
    ViewData["Title"] = "Menus";
}

<h1>Menus in @Model.RestaurantName</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Active From</th>
            <th>Active To</th>
            <th>Restaurant</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Menus)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.ActiveFrom.ToString("yyyy-MM-dd")</td>
                <td>@(item.ActiveTo.HasValue ? item.ActiveTo.Value.ToString("yyyy-MM-dd") : "-")</td>
                <td>@item.RestaurantName</td>
                <td class="text-end">
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-secondary">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
